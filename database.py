from sqlalchemy import Column, Integer, String, ForeignKey, DateTime\nfrom sqlalchemy.orm import relationship\nfrom sqlalchemy.ext.declarative import declarative_base\nimport datetime\n\nBase = declarative_base()\n\nclass User(Base):\n    __tablename__ = 'users'\n\n    id = Column(Integer, primary_key=True)\n    username = Column(String, unique=True)\n    email = Column(String, unique=True)\n    created_at = Column(DateTime, default=datetime.datetime.utcnow)\n\nclass PremiumSubscription(Base):\n    __tablename__ = 'premium_subscriptions'\n\n    id = Column(Integer, primary_key=True)\n    user_id = Column(Integer, ForeignKey('users.id'))\n    start_date = Column(DateTime)\n    end_date = Column(DateTime)\n    user = relationship('User', back_populates='subscriptions')\n\nclass SupportTicket(Base):\n    __tablename__ = 'support_tickets'\n\n    id = Column(Integer, primary_key=True)\n    user_id = Column(Integer, ForeignKey('users.id'))\n    created_at = Column(DateTime, default=datetime.datetime.utcnow)\n    status = Column(String)\n    user = relationship('User', back_populates='tickets')\n\nclass AdminLog(Base):\n    __tablename__ = 'admin_logs'\n\n    id = Column(Integer, primary_key=True)\n    action = Column(String)\n    user_id = Column(Integer, ForeignKey('users.id'))\n    timestamp = Column(DateTime, default=datetime.datetime.utcnow)\n    user = relationship('User', back_populates='logs')\n\nUser.subscriptions = relationship('PremiumSubscription', order_by=PremiumSubscription.id, back_populates='user')\nUser.tickets = relationship('SupportTicket', order_by=SupportTicket.id, back_populates='user')\nUser.logs = relationship('AdminLog', order_by=AdminLog.id, back_populates='user')\n